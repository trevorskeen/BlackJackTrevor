using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "CardSettings", menuName = "ScriptableObjects/CardSettings", order = 1)]
public class CardSettings : ScriptableObject
{
    public Sprite cardBackSprite; // This will store the sprite for the card back
}
